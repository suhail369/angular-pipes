import { CovertMilesPipe } from './covert-miles.pipe';

describe('CovertMilesPipe', () => {
  it('create an instance', () => {
    const pipe = new CovertMilesPipe();
    expect(pipe).toBeTruthy();
  });
});
